# NextJs + Next-Auth + TS + TypeORM + Postgres 

Basically you can run the docker compose on local and start development environment for a new project or learning propose quickly.

Start js_fullstack 
```
npm run dev
```